# P40 动态目标执行与外界打断系统复审报告

## 复审结论

结论：**通过复审，可以进入提交/验收候选**。

上次审核提出的 3 个 P0 问题均已修复，并补充了对应测试。P40 目前达到“目标执行模块最小闭环 + H5 可见执行状态 + 内部阻塞入栈 + 暂停恢复 driver 状态”的验收底线。

## P0 修复复查

### P0-1：H5 execution_status 实际接入 GoalExecutionSystem

复查结果：已修复。

证据：

- `backend/src/h5_playable/playable_turn_service.cpp:230` 新增 `buildExecutionStatus`。
- `backend/src/h5_playable/playable_turn_service.cpp:247` 构造 `ExecutionTickRequest`。
- `backend/src/h5_playable/playable_turn_service.cpp:256` 调用 `GoalExecutionSystem::tick`。
- `backend/src/h5_playable/playable_turn_service.cpp:357` 在 `buildResponse` 中生成执行状态。
- `backend/src/h5_playable/playable_turn_service.cpp:359` 写入 `response.execution_status`。
- `backend/tests/unit/h5_playable/playable_test.cpp:479` 增加 H5 可见执行状态测试。

说明：

- 现在 H5 不只是“有字段”，而是实际调用 P40 执行系统并返回 `visible == true`。
- 前端仍只消费后端字段，没有从回复文本推断执行状态。

### P0-2：内部阻塞子目标进入目标栈

复查结果：已修复。

证据：

- `backend/include/pathfinder/goal_execution/goal_execution_types.h:391` 新增 `generated_subgoals`。
- `backend/src/goal_execution/goal_execution_system.cpp:826` 解析内部阻塞生成子目标。
- `backend/src/goal_execution/goal_execution_system.cpp:828` 写入 `result.generated_subgoals`。
- `backend/src/goal_execution/goal_execution_system.cpp:832` 暂停原 active frame。
- `backend/src/goal_execution/goal_execution_system.cpp:846` 将子目标 frame push 入目标栈。
- `backend/tests/unit/goal_execution/goal_execution_test.cpp:140` 的钝斧测试断言生成 `RestoreToolState` 子目标且目标栈增加。

说明：

- “斧头钝 -> 打磨斧头”已经不是只生成后丢弃，而是进入目标栈。
- 当前只 push 首个子目标，符合最小实现；多阻塞排序可后续扩展。

### P0-3：恢复时恢复 active_driver_state

复查结果：已修复。

证据：

- `backend/src/goal_execution/goal_execution_system.cpp:380` 暂停时保存 `active_driver_state`。
- `backend/src/goal_execution/goal_execution_system.cpp:403` 恢复时把 paused driver_state 写回 `active_driver_state`。
- `backend/src/goal_execution/goal_execution_system.cpp:404` 恢复后移除 paused context。
- `backend/tests/unit/goal_execution/goal_execution_test.cpp:186` 断言恢复后 `active_driver_state->driver_kind == ChopWood` 且 paused context 清空。

说明：

- “狼退走后继续伐木”在执行层能恢复具体 Driver 状态，不再只恢复目标名。

## P1 修复复查

### P1-1：材料状态投影链路

复查结果：已补测试覆盖。

证据：

- 新增 `goal_execution_material_tags_from_dialog_runtime_snapshot` 测试。
- 测试从真实 `DialogScenarioCatalog` / `WorldSnapshotAdapter` 构造快照，验证 runtime 状态进入 P40 材料判断。

### P1-2：ReactionMaterialResolver 硬映射风险

复查结果：最小阶段可接受，已补边界和测试。

证据：

- 新增 `reaction_material_resolver_no_unknown_material_for_core_rules` 测试。
- 核心规则链路不再产出 `unknown_material`。

保留说明：

- 当前仍是 P28 fixture bridge 的最小过渡方案，后续应迁移到配置化 object definition / reaction content pack 查询。

### P1-3：报告测试路径

复查结果：主路径已修复。

证据：

- `doc/review/P40动态目标执行与外界打断系统自审报告.md` 已使用 `build/backend`。
- `doc/review/P40动态目标执行与外界打断系统复审修复报告.md` 主验证也使用 `build/backend`。

注意：

- 复审相关报告的测试路径已统一为 `build/backend`。

## 已运行验证

本次复审实际运行：

```text
cmake -S backend -B build/backend
cmake --build build/backend --target pathfinder_tests_goal_execution pathfinder_tests_h5_playable -j2
ctest --test-dir build/backend -R "goal_execution|h5_playable|h5_execution_status_visible_when_goal_execution_runs|internal_blocker_pushes_subgoal_frame|resume_restores_driver_state" --output-on-failure
```

结果：

```text
100% tests passed, 0 tests failed out of 40
```

补充回归：

```text
cmake --build build/backend --target pathfinder_tests_agent_reasoning pathfinder_tests_world_interaction -j2
ctest --test-dir build/backend -R "agent_reasoning|world_interaction" --output-on-failure
```

结果：

```text
100% tests passed, 0 tests failed out of 18
```

## 后续阶段任务

1. P41/P42：把 H5 最小可见计划升级为可持久化 `ExecutionContext`，接入会话、存档和复盘。
2. P41 多阻塞/调度：为多个 `generated_subgoals` 增加排序、排队和多 Agent 资源竞争处理。
3. P41 配置化内容：把 `ReactionMaterialResolver` 的 P28 fixture bridge 迁移到 object definition / reaction content pack 查询。
4. P41/P42 建造内容：补正式建筑 recipe，让 `BuildStructure` 从配置化 recipe 生成 `MaterialRequirementSet`。

## 最终判断

P40 作为“动态目标执行与外界打断系统”的最小架构阶段已经达标：

- 有目标栈。
- 有 Driver。
- 有内部阻塞转子目标。
- 有外界打断暂停当前目标。
- 有恢复 driver 状态。
- 有材料检查入口。
- 有 reaction 产出链入口。
- 有 H5 安全可见执行状态。
- 有关键回归测试。

建议：**可以验收 P40，后续进入提交/推送或继续下一阶段设计。**
