# P6 观察与动作空间构建复审报告

复审日期：2026-05-17  
复审对象：P6 ObservationBuilder / ActionSpaceBuilder 修复后实现  
复审结论：通过，允许进入 P7。

## 1. 复审范围

本次复审针对上次 P6 审核提出的阻塞问题逐项确认：

- 不存在的 visible object 是否从 `ok + warning` 修复为 error。
- P6 边界扫描是否干净通过。
- 隐藏真实效果扫描是否干净通过。
- cognition claim 查询是否从 Agent Builder 内部底层字段拼装，改为 cognition 模块语义查询接口。
- `backend/dev_notes/agent.md` 是否补充 P6 说明。
- P6 相关构建、定向测试、全量测试是否通过。

## 2. 复审结论

结论：P6 通过。

理由：

- 上次 3 个阻塞问题均已修复。
- `ObservationBuilder` / `ActionSpaceBuilder` 的生产代码边界符合 P6 任务卡。
- 自动化构建、定向测试、全量测试全部通过。
- P6 边界扫描命令全部无命中。
- 现有问题只剩非阻塞工程清洁项，不影响进入 P7。

## 3. 上次阻塞项复查

### BLOCKER-1：不存在 visible object 必须返回错误

状态：已修复，通过。

证据：

- `backend/src/agent/observation_builder.cpp:43` 遍历 `visible_object_ids`。
- `backend/src/agent/observation_builder.cpp:45` 检测 object 不存在。
- `backend/src/agent/observation_builder.cpp:46` 返回 `Result::fail`。
- `backend/tests/unit/agent/agent_observation_builder_test.cpp:95` 测试函数已改为 `test_nonexistent_object_errors`。
- `backend/tests/unit/agent/agent_observation_builder_test.cpp:101` 明确断言 `result.is_error()`。

判断：符合 `doc/31_P6观察与动作空间构建任务卡设计.md` 中“不存在 object_id 返回错误”的要求。

### BLOCKER-2：P6 边界扫描未通过

状态：已修复，通过。

本次执行：

```bash
rg -n "RulePipeline|EatObjectResolver|StateChange|EventRecord" backend/include/pathfinder/agent/observation_builder.h backend/src/agent/observation_builder.cpp backend/include/pathfinder/agent/action_space_builder.h backend/src/agent/action_space_builder.cpp
```

结果：无命中。

本次执行：

```bash
rg -n "GameState|state::" backend/include/pathfinder/agent/action_space_builder.h backend/src/agent/action_space_builder.cpp
```

结果：无命中。

本次执行：

```bash
find backend/include/pathfinder/agent backend/src/agent -type f \( -iname "*runtime*" -o -iname "*policy*" -o -iname "*rl_environment*" \) -print
```

结果：无输出。

本次执行：

```bash
rg -n "AgentRuntime|policy_tree|rl_environment|FleeResolver|GroupCombat|WarResolver|tribe_split|SaveManager|WebSocket|HTTP" backend/include/pathfinder/agent/builder_types.h backend/include/pathfinder/agent/observation_builder.h backend/include/pathfinder/agent/action_space_builder.h backend/src/agent/builder_types.cpp backend/src/agent/observation_builder.cpp backend/src/agent/action_space_builder.cpp backend/tests/integration/p6
```

结果：无命中。

判断：P6 未提前实现运行时、策略、RL、存档、前端网络层、真实逃跑/群战结算等后续阶段内容。

### BLOCKER-3：隐藏真实效果扫描未通过

状态：已修复，通过。

本次执行：

```bash
rg -n "edible_profile|hunger_delta|health_delta|effect_kind" backend/include/pathfinder/agent backend/src/agent backend/tests/unit/agent backend/tests/integration/p6
```

结果：无命中。

修复方式确认：

- `backend/src/agent/observation_builder.cpp:74` 使用 `CognitionState::findEdibleClaim`。
- `backend/src/agent/observation_builder.cpp:84` 使用 `CognitionState::findHarmfulClaim`。
- `backend/include/pathfinder/cognition/cognition_state.h:27` 提供语义查询接口。
- `backend/include/pathfinder/cognition/cognition_state.h:32` 提供语义查询接口。
- `backend/src/cognition/cognition_state.cpp:13` 实现食用认知查询封装。
- `backend/src/cognition/cognition_state.cpp:25` 实现风险认知查询封装。

判断：Agent Builder 不再直接拼装底层 cognition key 的隐藏效果字段，符合“观察层只拿主观认知摘要，不暴露世界真值”的设计方向。

## 4. 测试结果

本次执行：

```bash
cmake -S backend -B build/backend
```

结果：通过。

本次执行：

```bash
cmake --build build/backend
```

结果：通过。

本次执行：

```bash
ctest --test-dir build/backend -R "agent|p6|p5|p3|p4" --output-on-failure
```

结果：25/25 passed。

本次执行：

```bash
ctest --test-dir build/backend --output-on-failure
```

结果：118/118 passed。

## 5. 通过项确认

已确认通过：

- `ObservationBuilder` 只读状态，不执行规则结算。
- `ObservationBuilder` 对缺失 actor / object 均走 error。
- consumed / depleted object 仍按设计进入 skipped trace，不进入可交互 objects。
- `ActionSpaceBuilder` 只从 `AgentObservation` 构建候选动作，不读世界内部状态。
- P6 未引入 `AgentRuntime`、策略系统、RL 环境、HTTP、WebSocket、SaveManager。
- unknown fruit、spider fire、wolf pack 三条 P6 集成测试均通过。
- `backend/dev_notes/agent.md` 已补充 P6 builder 职责、目录、依赖边界和测试入口。

## 6. 非阻塞建议

### MINOR-1：Builder 单元测试仍链接 pipeline / rules

位置：

- `backend/tests/CMakeLists.txt:783`
- `backend/tests/CMakeLists.txt:786`
- `backend/tests/CMakeLists.txt:787`
- `backend/tests/unit/agent/agent_observation_builder_test.cpp:6`
- `backend/tests/unit/agent/agent_observation_builder_test.cpp:187`

判断：

- 这不影响 P6 生产代码边界，也不影响本次通过结论。
- 但 P6 设计更推荐把闭环 pipeline 验证放在 `backend/tests/integration/p6/`，unit 测试最好只构造最小状态和 cognition 数据，避免后续 AI 误学到“Builder 单元测试可以依赖完整管线”。

建议：

- 可以在 P7 前顺手清理，也可以作为后续测试清洁任务处理。
- 不作为 P6 阻塞项。

### MINOR-2：`backend/dev_notes/agent.md` 的 P5 边界和 P6 边界同时存在

判断：

- 当前文档已经能说明 P6 builder 可以只读 `GameState`。
- 但上方 P5 边界仍写着 Agent 不直接读写 `GameState`，对后续 AI 可能有轻微歧义。

建议：

- 后续可把文档拆成“P5 core agent 禁止读状态”和“P6 builder 允许只读状态”两层边界。
- 不作为 P6 阻塞项。

## 7. 最终结论

P6 复审通过，可以进入 P7。

P6 当前已经完成最小观察与动作空间构建能力：

- 从世界状态和可见性输入构建 Agent 主观观察。
- 从主观观察构建可选动作候选。
- 不执行规则结算，不替 Agent 做决策，不泄露世界真实效果。
- 为后续 AgentRuntime、策略、训练环境、行为回放打下了可扩展基础。
